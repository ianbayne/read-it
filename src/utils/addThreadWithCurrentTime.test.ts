import { describe, it } from "@jest/globals";

// import addThreadWithCurrentTime from "./addThreadWithCurrentTime";

describe("addThreadWithCurrentTime()", () => {
  it("adds the thread ID to Chrome storage with the current time", async () => {
    // TODO: Figure out how to test Chrome storage
  });
});
