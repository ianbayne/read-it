import { describe, it } from "@jest/globals";

// import storeThreadIdWithTime from "./storeThreadIdWithTime";

describe("storeThreadIdWithTime()", () => {
  it("adds the thread ID to Chrome storage with a time", async () => {
    // TODO: Figure out how to test Chrome storage
  });
});
